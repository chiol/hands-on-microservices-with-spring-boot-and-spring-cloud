server.port: 7002
server.error.include-message: always

logging:
  level:
    root: INFO
    io.github.chiol: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG


spring:
  data:
    mongodb:
      auto-index-creation: true
      host: localhost
      port: 27017
      database: product-db

---

spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080

spring.data.mongodb.host: mongodb
